import { defineConfig } from 'vite'
import path from 'path'

// ⚠️ CE FICHIER EST DÉSACTIVÉ - Utiliser vite.config.js à la place
// Raison: @vitejs/plugin-react@6.0.1 incompatible avec Vite 6.3.5

// Workaround pour incompatibilité @vitejs/plugin-react@6.0.1 avec Vite 6.3.5
// Le plugin essaie d'importer 'vite/internal' qui n'existe pas dans les exports

// https://vitejs.dev/config/
export default defineConfig({
  plugins: [],
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './'),
    },
  },
  esbuild: {
    jsx: 'automatic',
    jsxImportSource: 'react',
    jsxInject: `import React from 'react'`,
  },
  server: {
    port: 3000,
    open: true,
  },
  build: {
    outDir: 'dist',
    sourcemap: true,
  },
  optimizeDeps: {
    include: ['react', 'react-dom'],
    esbuildOptions: {
      jsx: 'automatic',
    },
  },
})
