// Moved to learning/ — this re-export keeps backward compatibility
export { CourseCard, default } from '../learning/CourseCard';
export type { CourseCardProps, CourseCardTone } from '../learning/CourseCard';
