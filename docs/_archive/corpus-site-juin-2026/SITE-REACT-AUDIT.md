# Site marketing React — Audit & redesign tracker

> **Créé** : 2026-06-10 · **Scope** : toutes les pages `src/pages/marketing/*.tsx` (routes à plat, clean-restart).
> **Méthode** : screenshot live (Chrome MCP, desktop 1470) + DOM/JS probes + lecture `.tsx` + comparatif des 6 variantes Home.
> **Dev** : `npm --prefix ~/Documents/Claude/Projects/frontend-tls run dev` → **http://localhost:5175/** (5173/5174 occupés).
> **Sources de vérité** : `PRODUCT.md`, `DESIGN.md`, `CLAUDE.md` (§ Marketing v2 + 13 pièges), `docs/_canon/FACTS-CANON.md` (⏸️ pricing gelé), `docs/_canon/AUDIT-COHERENCE.md`, `docs/site/DESIGN-INSPO.md`, `docs/marketing/*`.
>
> **On commence par la Home.** Les autres pages = backlog scaffold (§3), auditées une par une au fil du redesign.

## Journal de décisions
- **2026-06-11 (Session 2 — CinematicHero ✅ + routing nettoyé)** — Correction **7 liens cassés** dans `CinematicHero.tsx` + `MarketingHomeEditorial.tsx` (les CTAs pointaient `/formation`, `/learning-app`, `/contact` sans le préfixe `/marketing/`, redirigeant users non-auth vers `/auth/login`). **Tous les chemins corrigés** : `/formation` → `/marketing/formation`, `/conseil` → `/marketing/accompagnement`, `/learning-app` → `/marketing/learning-app`, `/contact` → `/marketing/contact`, `/blog` → `/marketing/magazine`. Offres data + sections Learning App + STRIDE callout + dark CTA = **tous propres** (`bad: []` en vérif live). **Vérif en live** : 24 liens marketing testés, tous en `/marketing/*` ✅. **Routing consolidé** : site marketing = strict `<Route path="/marketing" ... />` (PAS aplatissement dans App.tsx — la doc disait « routes à plat » mais la réalité = sous-préfixe). À confirmer avec Chloé si aplatissement `/marketing/*` → `/*` voulu (sera une décision direction).

- **2026-06-11 (Direction C — Hero cinématique Home ✅)** — Lancement **Direction C « Illustrated Glass »** (brief complet : `docs/site/DESIGN-INSPO.md` §Directrice + `docs/site/SITE-STRUCTURE-DESIGN-C.md`). **Nouveau composant `src/pages/marketing/components/CinematicHero.tsx`** = pièce maîtresse Home : (gauche) titre League Spartan massif « Ce que l'IA change pour ceux qui [forment→conçoivent→accompagnent→transmettent] » avec **word-swap Ditto** (AnimatePresence, slide+blur 0.55s `ease-emphasis`), sous-titre, dual-CTA (MagneticButton primary `/marketing/formation` + ghost `/marketing/learning-app`), **chips honnêtes** (Certifiant·Qualiopi / Open Badge / Éligible OPCO — 0 métrique) ; (droite) **panneau glass double-bezel** (Craft : tray `bg-white/40` ring + core `bg-white/55 backdrop-blur-glass-medium` inner-shadow refraction) encadrant la **boucle Learn→Do→Match** en SVG — 3 nodes color-codés (teal/orange/gold) reliés par **arcs hand-drawn qui se dessinent** (`pathLength`, Daydream), **blobs organiques qui respirent** (Phantom), **particules dorées** flottantes, status-pill « Passeport vérifiable » à pulse. Parallax scroll desktop (`useTransform`). **⚠️ Règle P0 respectée** : refactor `initial opacity:0` framer → **`SettleIn`** (opacity:1 toujours, transform-only) car le `initial:0` figeait le hero invisible en onglet backgrounded (re-confirme le gotcha mémoire). Mobile : stack vertical, pas de parallax, `min-h-[92dvh]`, 0 overflow horizontal, net en foreground. **`tsc` = 0**, **0 erreur console**, vérifié desktop+mobile (Preview MCP). **Stand-in cinématique** : tourne sans asset ; la vraie vidéo 10s s'y glissera (Phase 1 Procreate/Loom).
- **2026-06-11 (Header + Footer — REDESIGN haut de gamme ✅)** — Refonte **visuelle** des 2 composants marketing (skills high-end-visual-design / design-taste), **strictement tokens TLS** (League Spartan/Nunito, primary/secondary/accent, `rounded-pill`, `--ease-emphasis`). **`MarketingHeader`** : passage barre pleine-largeur → **shelf de verre flottant** (Fluid Island détaché `top-2.5`, double-bezel + refraction `shadow-[inset…]` + `ring-ink-900/5`, `backdrop-blur-glass-heavy`) ; **pill actif glissant** (`layoutId="nav-active-pill"`, spring) ; **scroll-reactive** (bg/shadow/border se renforcent >12px, transition GPU-safe bg/shadow only) ; CTA Contact **button-in-button** (pastille icône imbriquée) + **magnétique** ; dropdown Ressources **double-bezel** glass ; **burger morph→X** ; **overlay mobile plein écran glass** + **reveal staggered** des liens (display League Spartan). **`MarketingFooter`** : éditorial sombre **manifeste oversize** (« Pour que la formation se prouve sur le terrain. ») + **boucle Learn→Do→Match** en chips + logo ; **newsletter double-bezel** (coque pill + input transparent + **submit imbriqué** pastille accent) ; colonnes liens **underline-draw** au hover ; **watermark géant** « Learning Society » (`text-white/[0.035]`, texture) ; 2 lueurs ambiantes statiques + hairline top. Registre **vous**, **0 em-dash**, **0 prix**, nav canonique inchangée (Home·Learning App·Formation·Conseil·Ressources▾). `tsc` = **0**. ⚠️ Réf Figma Make écartée (header plat + contenu périmé « Académie »/« MAG' »). ⚠️ Vérif preview : header desktop **OK** (capture nette), footer **OK** (structure+style confirmés eval) ; captures below-fold flakey (throttle onglet backgrounded = gotcha connu) + **churn HMR** transitoire du codemod linter (`max-w-7xl`→`max-w-wide` sur plusieurs fichiers, `Project.tsx` cassé transitoirement puis OK sur disque). **Reste demande Chloé : passe visuelle main pages** (à enchaîner après son retour header/footer).
- **2026-06-11 (Conseil — REBUILD ✅)** — Nouvelle page `MarketingConseil` (`/conseil`, remplace `MarketingAccompagnement`) : hero light+warm, **Audit Flash SBO = porte d'entrée** (0,5–1j, 3 livrables), **STRIDE 6 étapes ordre canon R avant I** (table unifiée, badges step+word+icon), **bonus 1 an Learning App**, **2 Pôles** (Chloé Conception / Pierre-Armand Delivery, noms réels), CTA dark final. `vous`, **0 prix** (⏸️), 0 em-dash, 0 `FadeInWhenVisible`, `tsc`=0, 0 erreur console. `/methode` → redirect `/conseil`. `MarketingMethode`/`MarketingAccompagnement` = orphelins (suppression à confirmer).
- **2026-06-11 (Infographie SBO interactive ✅)** — Nouvelle page `MarketingInfographieSBO` (`/infographie-sbo`) : **scrollytelling 4 temps** (basculement Job→Skills · WEF ~60 % · Deloitte +52/+57 % · boucle Learn→Do→Match) via `StickyScrollStory` + `CountUp`, lane light+warm, **chiffres attribués WEF/Deloitte** (0 métrique TLS), lien vers le dossier complet + CTA dark. Route câblée + carte **« Infographie SBO »** dans le hub `/ressources` (grille passée en 2-col propre). `tsc` = **0**. ⚠️ Le preview Claude (port 5173) pointait sur `~/Projects/frontend-tls` = **stub supprimé** pendant la session → vérif visuelle à refaire sur le **dev réel (5175, `~/Documents/Claude/...`)**.
- **2026-06-11 (Catalogue Motion & DS — refait propre ✅)** — `MarketingMotionLab` (`/_motion-lab`) → **catalogue vivant** : chaque primitive **nommée + « quand l'utiliser » + démo live** (Reveal · MeshGradientBg · ParallaxLayer · MagneticButton · **TiltCard** · MarqueeRow · CountUp · **NoiseTexture** · **ScrollProgress** · InteractiveAppMockup · StickyScrollStory). **Nettoyé** : faux logos clients **(dont Orange)** retirés → concepts maison ; **GradientText + FadeInWhenVisible** sortis (section « ⛔ Dépréciés », pointent vers `Reveal`/couleur solide) ; **métriques inventées** (97 %/200+/40+) → stats marché **sourcées Deloitte** (52/57 %) ; registre **vous**. `tsc` = **0**. → sert de **référence vocabulaire** pour nos échanges design.
- **2026-06-10 (Dossier enrichi — patterns LA ✅)** — `MarketingDossierDetail` : + **ReadingProgressBar** (`useScroll`/`useSpring`, fixe en haut) · **IntroCallout** « En résumé » (résumé exécutif, callout warm) · section **« Points clés »** (grille 2-col, 4 takeaways **sourcés** Deloitte/MIT Sloan/Bersin/TLS — champ `keyFindings` ajouté au type + dossier SBO). Download PDF **différé** (pas de bouton mort tant qu'il n'y a pas de PDF réel). `tsc` = **0**. ⏳ Suite : **Infographie SBO interactive** (pièce signature).
- **2026-06-10 (Blog index — refonte ✅)** — `MarketingMagazine` : routes cassées `/marketing/magazine/*` → **`/blog/*`** (P0) · registre **tu → vous** (hero, empty-state, newsletter, toast) · `FadeInWhenVisible` → **`Reveal` local** (visible par défaut → fix hero-vide) · emoji ✨ retiré. Structure déjà on-direction conservée (featured + grille cartes **dégradé DS** + `BookOpen`, badges tons primary/warm/sun, filtres + search). `tsc` = **0**. ⏳ Suite : **Article detail** (`MarketingArticleDetail` : routes `/blog/`, retirer **`GradientText`** banni, `FadeInWhenVisible`→`Reveal`) puis aligner le **Dossier** dessus. Images : couvertures = **dégradé DS** pour l'instant (fallback validé) ; génération d'images on-brand = passe imagegen ultérieure.
- **2026-06-10 (Nav + Footer refaits — DS-align ✅)** — `MarketingHeader` : `window.addEventListener('scroll')` → `useScroll`/`useMotionValueEvent` (anti-pattern banni retiré) · **Dossiers** ajouté au menu Ressources · dégradés décoratifs (soulignement actif + badge « New ») → **tokens solides** (`bg-primary-600` / `bg-secondary-500`). `MarketingFooter` : **link-farm 4 col → 3 zones** (brand+newsletter / Offres / Ressources) · **2 rangées trust redondantes → 1** (`SOCIAL_PROOF` supprimé) · copy « **Transformez** » (mot banni) → « Pour que la formation se prouve sur le terrain » · Contact **dédupliqué** · **Dossiers** ajouté · légal en `<Link>` (plus `<a>`) en barre basse · blob animé infini → lueur **statique** · emoji ✨ retiré. `tsc` = **0 erreur**. ⏳ Reste (demande Chloé) : redesign pages **Blog/Article/Mag** inspiré des pages **veille de la Learning App** (`Veille`/`Magazine`/`MagazineArticle` + patterns `VeilleCardFeed`/`MagazineCard`) + Figma « **Website TLS - Figma Make** » (besoin du **lien/sélection Figma**).
- **2026-06-10 (Ressources/Dossiers — build ✅)** — Système **Dossiers** créé (contenu de fond sourcé, distinct du Blog). Data `src/data/marketingDossiers.ts` (type `Dossier` + `sources`/`sourceCount`) avec le **dossier SBO** (7 chapitres, données **attribuées** Deloitte +52 %/+57 % · WEF ~60 %, **18 sources** en biblio, **0 métrique TLS**). Pages : `MarketingDossierDetail.tsx` (hero light+cover, **sommaire sticky**, corps chapitré + bloc `stat`, section **Bibliographie**, 1 CTA dark) et `MarketingDossiers.tsx` (index featured + grille). **Reveal LOCAL visible-par-défaut** (pas de `FadeInWhenVisible` → pas de bug hero-vide), **0 GradientText**, `Button` enveloppé dans `<Link>` (pas de prop `as`). Routes à plat `/dossiers` + `/dossiers/:slug` (App.tsx). Hub `/ressources` enrichi (carte **Dossiers** + dossier SBO à la une). `tsc --noEmit` = **0 erreur**. Matière : Drive `marketing/contenu veille` (dossier SBO + [biblio 80 sources](https://drive.google.com/file/d/19_GeuD4otiBEzakkF5unDR-6kiK3dz6S/view)). ⏳ Reste : prose du dossier à affiner (passe copy) ; **Conseil** ancré (FAITS-OFFRES §4 : Audit Flash + Pôles) mais **page pas encore rebuild** ; ordre **STRIDE = Réaliser avant Intégrer** (canon validé ; page Notion à corriger).
- **2026-06-10** — Convergence variantes Home : on **garde Preserve + Editorial**, on **supprime A / B / C / Bold** (fichiers effacés). **`/` (home) = `MarketingHomePreserve`** (avant : `MarketingHome` « Immersive Fusion »). `MarketingHome.tsx` = **retiré du routing** (orphelin, fichier encore sur disque — suppression à confirmer). Editorial conservé en référence (`/home-editorial`). → **Base du redesign = Preserve** (plus le squelette Editorial proposé §1.4/§2 ; à arbitrer au lancement du rebuild). Build/console vérifiés : runtime **0 erreur**, type-check **OK** côté routing.
  - Dette connue de Preserve (devient celle de `/`) : **GradientText ×5** (banni) · registre **tu** · **hero vide au chargement** (45 div à `opacity:0`, même bug B1) · pill **« Open Badge 2.0 »**. Voir fix-list §2.
  - ⚙️ Détecté en marge : **erreur TS pré-existante** `MarketingLearningApp.tsx:298` (bloque `npm run build`, sans rapport avec la Home) — à traiter à l'audit Learning App.
- **2026-06-10 (révision)** — Finalement **`/` = `MarketingHomeEditorial`** (et non Preserve). **Base du redesign = Editorial** (le plus propre : **0 GradientText**, modèle `Reveal` non-gaté → **hero visible au chargement**, lane LIGHT). Preserve gardé en réf (`/home-preserve`). Greffes prévues dessus : bande conviction (C), scroll-story système (Bold), chapitre humain (Preserve). `MarketingHome.tsx` toujours orphelin (suppression non tranchée).
- **2026-06-10 (REBUILD Home ✅ — fait, à valider par Chloé)** — Home `/` (`MarketingHomeEditorial.tsx`) retravaillée. Décisions Chloé : **pas de photos** (fondateurs ni About — About en dernier) → humain porté par la voix + l'UI produit, slot photo prêt pour plus tard ; boucle nommée **Learn → Do → Match**. Appliqué : registre **vous** partout · **0 métrique inventée** (97 %, 200+, 40+ retirés) · **Open Badge** (plus « 2.0 ») · **CPF jamais / OPCO affiché** (FACTS-CANON F10) · **0 AI Act** · routes **à plat** · slots preuve honnêtes (« un grand groupe français », jan 2026 ; témoignages différés « avec accord, pas avant »). Pièce maîtresse : **scroll-story Learn → Do → Match** (`StickyScrollStory` + nouveau composite `src/pages/marketing/components/LearnDoMatchVisual.tsx` — Dreyfus / projet réel / passeport, stepper morphing) = « montrer un système ». Hero light + ambient warm subtil, bande conviction teal, demo produit (`InteractiveAppMockup`), offres en **index tone-codé**, callout STRIDE→`/conseil`, **1 seule** section dark (CTA final). **Footer** corrigé aussi (OB, CPF/OPCO, AI Act, routes à plat, vous). `tsc` propre hors erreur pré-existante `MarketingLearningApp.tsx:298` (bloque `npm run build`). **0 erreur console.**
  - ⚠️ **Gotcha vérif Chrome MCP** : l'onglet piloté est *backgrounded* (`document.hidden=true`) → le navigateur **met en pause** les anims framer/WAAPI → page rendue **~40 % d'opacité** (« délavée ») ET le scroll-progress framer **ne se met pas à jour** (la sticky-story reste figée sur le 1er panneau). **Ce n'est PAS un bug** : en foreground (vrai utilisateur) tout est net et la story morphe. Pour screenshoter proprement : exécuter `document.querySelectorAll('*').forEach(el=>el.getAnimations().forEach(a=>{try{a.finish()}catch(e){}}))` juste avant la capture ; pour voir les 3 beats de la story, forcer la version mobile (`section.lg:hidden` → `display:block`) qui rend `visual(0..2)` sans dépendre du scroll. *(Le « hero délavé » de l'audit initial = en partie ce gotcha + en partie le vrai gating opacity:0 de l'ancienne `FadeInWhenVisible`, corrigé.)*
- **2026-06-10 (MAJ post-rebuild)** — Suite retours Chloé : **erreur TS `MarketingLearningApp.tsx:298` corrigée** (`cloneElement` typé → `tsc` = **0**, `npm run build` débloqué) · ancienne **`MarketingHome.tsx` supprimée** · **OPCO ré-affiché** (F10 : OPCO oui / CPF jamais) sur le rail hero + footer · **copy deck** créé pour validation section par section : [`docs/site/COPY-HOME.md`](COPY-HOME.md).

## Légende priorité
- **P0** — bloquant (bug rendu, lien cassé, violation honnêteté dure, contraste AA).
- **P1** — important (registre, anti-slop, asset hors-marque, copy IA non validée).
- **P2** — polish (cohérence fine, micro-contenu, opportunités motion).

---

## 1. HOME — `/` = `MarketingHome.tsx` (« Immersive Fusion »)

### 1.1 Verdict
Bonne **intention** (hero immersif teal → demo produit → chapitre humain → CTA dark) et **0 GradientText** (propre sur ce point, contrairement à Bold/Preserve). Mais la page est **cassée au chargement** (hero invisible), **délavée** (contraste), **off-registre** (tu, alors que Home = vous B2B), avec **liens 100 % cassés** (tous en `/marketing/*` → redirigés vers `/`) et **3 violations honnêteté** (Open Badge 2.0, CPF/OPCO, photo stock + citation anonyme). Direction visuelle (drench teal sombre) **hors-lane** : la lane validée est **LIGHT + warm**, pas dark-SaaS.

### 1.2 Bugs techniques confirmés (preuve)
| # | Prio | Bug | Preuve | Cause | Correctif |
|---|---|---|---|---|---|
| B1 | **P0** | **Hero vide au chargement** (header OK, dessous = néant) | DOM : `<h1>` opacity 1 MAIS un `<div>` ancêtre à `opacity:0` ; contenu n'apparaît qu'au moindre scroll | `FadeInWhenVisible` (`whileInView`, margin `-80px`) ne se déclenche jamais pour le 1er viewport (éléments déjà in-view au mount) → **CLAUDE.md piège connu**. Aggravé par `useTransform(scrollY,[0,400]→[1,0.6])` sur le wrapper. | **Ne jamais gater la visibilité sur un reveal.** Adopter le modèle `Reveal` d'Editorial (opacity:1 par défaut, seul `translateY` s'anime) OU patcher `FadeInWhenVisible` (prop `eager`/above-fold) au niveau DS. |
| B2 | **P0** | **Hero délavé / contraste faible** | Screenshot : hero gris-vert pâle, pas teal foncé | `MeshGradientBg tone="brand" intensity="intense"` peint de gros blobs clairs par-dessus le fond `from-primary-700…900` → lave le haut du hero ; texte blanc/`accent-300` y passerait sous AA | Hero LIGHT (cf. plan §2) ou mesh `subtle` + base contrastée ; valider AA. |
| B3 | **P0** | **Tous les CTA/liens du hero & sections bouclent sur la Home** | Liens `to="/marketing/formation\|learning-app\|magazine\|contact"` + route `/marketing/* → <Navigate to="/" />` (App.tsx:538) | Routes périmées (avant clean-restart) | Slugs à plat : `/formation`, `/learning-app`, `/blog`, `/conseil`, `/contact`. « Lire le magazine » → « Lire le blog ». |

### 1.3 Tableau d'audit par section
| Section | Contenu (honnêteté / registre / copy IA) | Design & usage DS | Motion / immersif | Anti-slop / bugs | Composite à créer/étendre | Prio |
|---|---|---|---|---|---|---|
| **1. Hero** (drench teal) | Pill **« Open Badge 2.0 »** ❌→ « Open Badge ». Trust strip **« CPF / OPCO éligible »** ❌ banni → retirer. Registre **tu** (« Développe **ta** pratique ») → **vous**. H1 « La formation augmentée par l'IA » OK. Mot accent = `text-accent-300` **solide** (✅ pas de gradient-text). | Tokens propres (primary/accent, `rounded-pill`, spacing sémantique). Fond `from-primary-700…900` = **dark, hors-lane LIGHT**. `Button` core réutilisé. | `MeshGradientBg`, `NoiseTexture`, 2×`ParallaxLayer`, `FadeInWhenVisible`, `MagneticButton`, scroll-scale. **Trop d'effets** + reveal qui casse. | **B1, B2** (P0). Contraste blanc/`white/85` sur fond pâle. | `HeroEditorialMkt` (light, sans gate) | **P0** |
| **2. Marquee « logos »** | Labels **génériques** (« Groupe industriel A », « Banque partenaire »…) + disclaimer « Logos illustratifs ». Plus honnête que A/B (pas de vraies marques) MAIS lit comme un **faux mur de logos** ; « logos illustratifs » sur du **texte** = confus. | `MarqueeRow`, tokens ink. | Marquee infini OK. | Pattern « faux logo wall » à éviter ; pas de preuve sociale réelle. | `HonestProofStrip` (slot preuve honnête, prêt pour vrais clients) | **P1** |
| **3. Demo produit** | « Essai**e**-la avant d'en parler à **ton** équipe », « **ton** métier » → **vous**. « demo call » (anglicisme léger). Checklist features = factuel ✅ (Dreyfus, coaching 1-1, journal, veille). | `InteractiveAppMockup` (**asset fort**, « montrer pas raconter »), `TiltCard`, halo glow. Tokens OK. | TiltCard + mockup jouable = bon. | RAS majeur. | Garder `InteractiveAppMockup` | **P1** |
| **4. Chapitre humain** (warm) | **Photo stock « femme qui sourit »** = anti-référence « corporate e-learning ». Citation **anonyme** « Je ne forme plus… » présentée comme réelle → marquer illustratif/retirer. « Lire le magazine » → « Lire le blog ». « On apprend avec des humains » ✅ (registre on/nous OK). | **Section la plus réussie** : fond warm `secondary-50/accent-50` (✅ on-lane), squiggly-underline SVG draw, portrait + floating quote card, tokens propres. | `motion.path` pathLength draw, `ParallaxLayer`, floating card reveal. Dosé, bon. | Photo anti-ref ; quote non sourcée. | `WarmHumanChapter` (image réelle/fondateurs) | **P1** |
| **5. Metrics + témoignages** | Anchors **factuels** ✅ (Qualiopi, Coaching 1-1) — bonne approche « pas de métrique inventée ». **« Open Badge 2.0 »** ❌→ OB. **« 7 modules · 7h »** : 7h non confirmé (gelé) → éviter le chiffre. Témoignages **cachés** (`false &&`) ✅ honnête. | Cards `gradient-to-br` tintées, tokens OK. | `FadeInWhenVisible` stagger. | OB 2.0 ; chiffre 7h non validé. | (réutiliser cards DS) | **P2** |
| **6. CTA final** (dark mesh) | « comprendre **tes** enjeux » → **vous**. « Réserver un échange » ✅. « Voir la démo ». | Fond `from-ink-900…` = **la** section dark (OK : 1 moment dark autorisé). `MeshGradientBg ink`. | Mesh + parallax + MagneticButton. | RAS. | (garder comme moment dark unique) | **P2** |

### 1.4 Comparatif des 6 variantes (`/home-a|b|c|editorial|bold|preserve`)
| Critère | A | B | C | Bold | **Editorial** | Preserve |
|---|---|---|---|---|---|---|
| Force éditoriale | moy | faible | haute | moy+ | **très haute** | haute |
| Motion | 0 | 0 | 0 (CSS) | très haut | **min. sûr (Reveal+IO)** | haut |
| Honnêteté | ❌ faux logos Renault/Orange + témoins nommés | ❌ idem + 97 % | ⚠️ témoins prénommés, OB 2.0 | ⚠️ OB 2.0 ; disclaimers OK | **✅ la + propre** (role-only, disclaimer ; OB 2.0 ×1) | ⚠️ OB 2.0 ; disclaimer OK |
| Registre | neutre | neutre | ❌ tu (structurel) | ❌ tu | ❌ tu (copy) | ❌ tu (copy) |
| Gradient-text **banni** | ❌ H1 | ✅ | ✅ | ❌ ×5 | **✅** | ❌ ×5 |
| Dark-SaaS / hype | léger | moy (dense) | non | « avant qu'elle ne te dépasse » | **non** | non |
| Token discipline | bon | bon | bon | excellent | **excellent** | excellent |
| Fit lane LIGHT+warm | moy | faible | haute | moy (dark lourd) | **haute** | moy+ |

**Reco : converger sur `MarketingHomeEditorial` comme squelette** (seul à cumuler : 0 gradient-text, 0 faux logo de marque, meilleure discipline tokens, lane LIGHT, idée éditoriale forte = offres en **index numéroté**, motion sûre = contenu jamais à opacity 0). Le « tu » touche **les 6** également → c'est une passe copy, pas un critère de squelette. **À écarter** comme base : A/B (faux logos de marque **en dur**), C (tu **structurel** + route périmée), Bold/Preserve (GradientText **structurel** + tu).

**Greffes (best-of) sur le squelette Editorial :**
- **de C** → la **bande conviction** `bg-secondary-500` (manifeste 1 ligne) = la chaleur qui manque à Editorial.
- **de Bold** → le **`StickyScrollStory` + `DoctrineVisual`** (cartes stat qui morphent) = LE moment scroll-storytelling « montrer un système » (réf Legora aOS). + le footnote honnête du proof-strip.
- **de Preserve** → le **chapitre warm** portrait + floating quote (« humain dans le cadre ») + icônes tone-codées (primary/warm/sun) par offre.
- **de A** → le **chiffre fantôme oversize** (`text-[5rem] text-primary-100`) comme accent typo dans l'index des offres.

---

## 2. Plan de redesign HOME (direction convergée)

**North Star** : *Crédibilité sobre + « montrer, pas raconter ».* Light · warm · BOLD sans (League Spartan) · **1** scroll-story qui montre un **vrai système** · **1** moment dark · humain dans le cadre · motion dosée · **contenu jamais gaté**. Registre **vous** (B2B) + impératifs neutres. **Aucun prix** (gelé). **Open Badge** (pas « 2.0 ») côté formation.

### 2.1 Flow de sections proposé
1. **Hero LIGHT warm-éditorial** — fond `bg-gradient-page-ambient` / `soft-pastel`, H1 `ink-900` (contraste AA), titre impératif neutre (*« Maîtrisez l'IA dans vos formations. »* — draft), sous-titre, **2 CTA** (Formation = primary `/formation` · Découvrir la plateforme = secondary `/learning-app`), chips honnêtes **« Certifiant · Qualiopi · Open Badge »** (sans 2.0, sans CPF/OPCO). Motion : `MeshGradientBg subtle` + `MagneticButton`, **reveals visibles par défaut**. → corrige B1/B2/B3.
2. **Slot preuve honnête** (remplace le faux mur de logos) — ligne sobre : *« Premiers déploiements en cours — un grand groupe français, depuis janvier 2026 »* (FACTS) + Qualiopi/C-Campus ; **prêt** pour vrais logos/témoignages. Zéro faux logo.
3. **Montrer le produit** — `InteractiveAppMockup` (garder) + texte/checklist (vous). « Montrer pas raconter ».
4. **Scroll-story signature (LE moment)** — `StickyScrollStory` qui visualise un **vrai système** : la **boucle d'apprentissage** (Apprendre · Concevoir · Déployer — *cf. décision naming*) **ou** le **Passeport Dreyfus 1→5**. Réf Legora : montrer un système, pas de la déco.
5. **Chapitre humain warm** — conviction « On apprend avec des humains, accompagnés par des outils » + portrait **réel** (pas stock) + floating quote (réelle ou omise). Fond warm light.
6. **3 offres** — Formation / **Conseil (STRIDE)** / Learning App — en **index numéroté éditorial** (greffe Editorial), accents tone-codés, **routes correctes**, **pas de pricing**.
7. **Teaser STRIDE** — 6 étapes, **ordre S·T·R·I·D·E** (Réaliser **avant** Intégrer — fix FACTS M3).
8. **CTA final = LA section dark** — carte dark contenue (modèle Editorial), « Réserver un échange » (vous), `MagneticButton`.
9. Footer (existant `MarketingFooter`).

### 2.2 Composites à construire (bottom-up, AVANT la page) → maj `Components.tsx`
- **Fix DS `FadeInWhenVisible`** : mode `eager`/above-fold pour ne **jamais** laisser le contenu à `opacity:0` (bénéficie à tout le site). *(ou primitive `Reveal` style Editorial)*
- **`HeroEditorialMkt`** — hero light warm (chips credential, dual-CTA, reveals non gatés).
- **`HonestProofStrip`** — preuve sobre sans faux logos, prête pour vrai contenu.
- **`LoopSystemStory`** — wrapper `StickyScrollStory` + `DoctrineVisual` (boucle / Passeport).
- **`OfferIndexRow`** — ligne d'offre numérotée éditoriale, tone-codée.
- **`WarmHumanChapter`** — portrait + floating quote + conviction (image réelle-ready).

### 2.3 Décisions ouvertes (à trancher — certaines = FAITS non validés)
- **D-A · Direction squelette** : Editorial + greffes *(reco)* / faire évoluer la Home actuelle / autre variante.
- **D-B · Hero** : LIGHT warm-éditorial *(reco, fixe contraste + lane)* / garder dark immersif.
- **D-C · Image « humain dans le cadre »** : photos **fondateurs** (Chloé / Pierre-Armand) / photo réelle sourcée non-cliché / produit-led + placeholder humain différé.
- **D-D · Naming boucle + angle** *(FACT ❓ non validé, AUDIT S2/S3)* : « Apprendre·Concevoir·Déployer » (live code) vs « Learn→Do→Match » (FACTS) ; angle Home = **pédagogie-IA/formation** (S3), **pas** SBO. → copy en draft, à valider.
- **Rappel** : **pricing/business model GELÉS** → la Home n'affiche **aucun** prix.

---

## 3. Backlog autres pages (scaffold — à auditer une par une)
| Page | Route | Fichier | Statut |
|---|---|---|---|
| Formation | `/formation` | `MarketingFormation.tsx` | ⏳ à auditer |
| Learning App | `/learning-app` | `MarketingLearningApp.tsx` | ⏳ à auditer |
| Conseil (= ex-Accompagnement + Méthode) | `/conseil` | `MarketingConseil.tsx` | ✅ REBUILD 2026-06-11 (ancien Accompagnement/Methode = orphelins) |
| Ressources (hub) | `/ressources` | `MarketingRessources.tsx` | ⏳ à enrichir (minimal) |
| Blog (= ex-Magazine) | `/blog` `/blog/:slug` | `MarketingMagazine.tsx` (+ `MarketingArticleDetail.tsx`) | ⏳ à auditer |
| About us (= ex-Équipe) | `/about` | `MarketingEquipe.tsx` | ⏳ à auditer (équipe réelle only) |
| Contact | `/contact` | `MarketingContact.tsx` | ⏳ à auditer |
| Légal ×4 | `/mentions-legales` `/politique-confidentialite` `/cgv-cgu` `/charte-ia` | `MarketingLegal.tsx` → 4 composants | ⏳ à auditer |
| _hors-nav_ : Témoignages (différé), `_motion-lab` (dev), 6 variantes Home | `/temoignages`, `/_motion-lab`, `/home-*` | — | hors-scope nav (réf seulement) |

> Contraintes transverses (toutes pages) : **vous** (B2B) / **tu** (pages Formation only, à arbitrer) · **Open Badge** ≠ « 2.0 » · pas de CPF/OPCO/Académie/AI-Act · Orange = « un grand groupe français » · zéro faux logo/témoignage (slots honnêtes) · équipe = Chloé Mimault + Pierre-Armand Dennery · **pricing gelé** · tokens DS partout (0 hex) · WCAG AA · `min-h-touch` · reveals jamais gatés.
