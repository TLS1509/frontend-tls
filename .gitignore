# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment files (local development config)
.env
.env.local
.env.*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.claude/worktrees/

# Local-only Notion spec sources (not for commit — contain proprietary cahiers)
CDC/
User_Journeys/
sitemap_fo_learning_app_sbo*.html
IMPLEMENTATION_ORDER.md

# MCP server config (contains API keys — never commit)
.mcp.json

# ── Brand assets — binaires lourds gardés en local (decks/docs régénérables via _pipeline) ──
# On garde le source léger (scripts _pipeline/*.js, *.py, READMEs, guidelines .md,
# fonts d'_pipeline/assets, signatures .html) ; on exclut les artefacts binaires.
# Chemins mis à jour après consolidation brand/ (2026-06-30)
*.pptx
*.pdf
brand/decks/**/*.jpg
brand/decks/**/*.jpeg
brand/decks/**/*.png
brand/docs/**/*.jpg
brand/docs/**/*.png
brand/assets/app-screenshots/
brand/assets/**/*.png
brand/assets/**/*.mp4
brand/assets/**/*.mov
# …mais conserver les assets source du pipeline (fonts + inputs de build)
!brand/decks/_pipeline/assets/
!brand/decks/_pipeline/assets/**
!brand/assets/video/_pipeline/assets/
!brand/assets/video/_pipeline/assets/**
# Livrables brand finaux : tracker les .pptx/.pdf (décision 2026-06-30)
!brand/decks/*.pptx
!brand/decks/*.pdf
!brand/docs/*.pptx
